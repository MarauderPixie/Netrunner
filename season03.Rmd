---
title: "Netrunnerliga Bremen - Season 3"
date: "`r ddateR::poee()`"
output:
  html_document
---

```{r, init, echo = F, warning = F, message = F}
source(paste0(Sys.getenv("PROJECT_HOME"), "./init.R"))
knitr::opts_chunk$set(fig.path = "assets/liga-plots/03")

liga_03         <- filter(liga, Saison == 3)
runner_liga_03  <- filter(runner_liga, Saison == 3)
konzern_liga_03 <- filter(konzern_liga, Saison == 3)
```


## Punktestand

Wer mehr als 80% seiner Spiele schon hinter sich hat, bekommt ne grüne Zahl. Bei weniger als 50%, erscheint ne rote.  

```{r, Punktestand}
  liga_03 %>% 
    group_by(Spieler) %>% 
    summarize(Spiele       = n()/2,
              Agendapunkte = sum(Agendapunkte, na.rm = T),
              Punkte       = sum(Punkte)) %>%
    arrange(desc(Punkte), desc(Agendapunkte)) %>% 
    format_table(
      list(Agendapunkte = color_tile("white", "lightgray"),
                 Spiele = formatter("span", style = x ~ style(
                                      color = ifelse(x <= 4, "red", 
                                              ifelse(x >= 7, "green", "gray"))))
           ),
      align = "l", check.rows = F, format = "markdown")
```

## Spielübersicht

Wer hat schon gespielt? Welche Spiele stehen noch aus? Vorschläge, das übersichtlicher zu gestalten?

```{r Spiele}
games <- liga_03 %>% 
 filter(Runde == 1) %>% 
 select(Spiel, Spieler)

games <- table(games$Spiel, games$Spieler)
games <- as.data.frame.matrix(!!crossprod(games))

yesno <- formatter("span", style = x ~ style(color = ifelse(x, "green", "red")),
                   x ~ icontext(ifelse(x, "ok", "remove")))

format_table(games, 
             list(Aschkan = yesno, Boray = yesno, Falk = yesno, Jan = yesno, 
                  Johannes = yesno, Josh = yesno, Kai = yesno, Ole = yesno, 
                  Paul = yesno, Tobias = yesno), 
             row.names = T, align = "c")
```

## Spielhäufigkeiten

### nach Fraktion

```{r, IDs}
  ggplot(liga_03, aes(x = Fraktion, fill = Fraktion)) +
    geom_bar(color = "black", alpha = 0.6) +
    labs(x = "", y = "Spiele") +
    facet_grid(. ~ Seite, scales = "free_x") +
    scale_fill_manual(values = c("darkorange", "deepskyblue3", "darkorchid4", "red", 
                                 "gold1", "darkgray", "chartreuse3", "darkolivegreen")) +
    cosmetics +
    theme(legend.position = "none")
```
  
### nach ID

```{r, IDs_Konzern}
  konzern_liga_03 %>%
    ggplot(aes(x = ID, fill = Fraktion)) +
      geom_bar(color = "black", alpha = 0.6) +
      labs(x = "", y = "Spiele", legend = "") +
      facet_grid(. ~ Fraktion, scales = "free") +
      scale_fill_manual(values = kon_cols) +
      cosmetics +
      theme(axis.text.x = element_text(angle = 25, hjust = 1, vjust = 1),
            legend.position = "none")
```

```{r, IDs_Runner}
  runner_liga_03 %>% 
    ggplot(aes(x = ID, fill = Fraktion)) +
      geom_bar(color = "black", alpha = 0.6) +
      labs(x = "", y = "Spiele") +
      facet_grid(. ~ Fraktion, scales = "free") +
      scale_fill_manual(values = c("darkorange", "deepskyblue3",
                                   "darkgray", "chartreuse3")) +
      cosmetics +
      theme(axis.text.x = element_text(angle = 25, hjust = 1, vjust = 1),
            legend.position = "none")
```


## Runs

### Runs pro Spieler

Der graue Balken ist die durchschnittliche Anzahl Runs über alle Spieler hinweg und die schwarzen Balken sind entsprechend der Durchschnitt der jeweiligen Person.

```{r, Runs_Spieler}
ggplot(runner_liga_03, aes(x = Spieler, y = Runs, color = Spieler)) +
  geom_point(position = "jitter") +
  stat_summary(fun.y = "mean", fun.ymax = "mean", fun.ymin = "mean", 
               colour = "black", size = 0.5, geom = "errorbar") +
  geom_hline(yintercept = mean(runner_liga_03$Runs, na.rm = T), size = 1, color = "gray") +
  labs(x = NULL, y = "Runs") + 
  # scale_color_brewer(palette = "Set1") +
  cosmetics + theme(legend.position = "none")
```

#### Mehr vom Gleichen, nur anders

So mit Mouse-Over-Informationen und allem. Bin aber noch am rumprobieren.

```{r, Runs_Spieler_rbokeh}
mittelrun_03 <- runner_liga_03 %>% group_by(Spieler) %>% summarize(Mittel = mean(Runs))

figure(title = "Runs pro Spieler", ylab = "Runs", xlab = NULL, tools = NULL,
       xgrid = F, ygrid = F, legend_location = NULL) %>% 
  ly_lines(x = Spieler, y = mean(runner_liga_03$Runs), data = runner_liga_03,
           color = "gray", alpha = 0.8) %>% 
  ly_points(Spieler, Runs, data = runner_liga_03, 
            hover = "@Runs Runs als @ID gegen @Gegner_ID",
            glyph = "diamond", color = Fraktion, size = 15) %>% 
  ly_points(Spieler, Mittel, data = mittelrun_03, glyph = "x", color = Spieler, size = 20)
```

### Runs auf Systeme pro Spieler

```{r runs_server_player}
runner_liga_03 %>%
  gather(Systeme, Sysruns, Archiv, `R&D`, HQ, Remote) %>%
  group_by(Spieler, Systeme) %>% 
  summarize(Sysruns = sum(Sysruns, na.rm = T)) %>% 
  ggplot(aes(x = Spieler, y = Systeme, fill = Sysruns)) + 
    geom_tile(color = "white", size = 0.8) +
    labs(title = "Summe der Runs aller Spiele pro Spieler und System",
         x = NULL, y = NULL) +
    scale_fill_viridis(option = "D") +
    cosmetics + 
    theme(legend.position = "bottom")
```

### Runs auf Systeme pro Fraktion

Waffelplots! 

```{r runs_server_faction}
hq <- runner_liga_03 %>% 
  select(Fraktion, HQ) %>% 
  group_by(Fraktion) %>% 
  summarize(Runs = sum(HQ, na.rm = T)) %>% 
  spread(Fraktion, Runs)

# for percentages, rather than absolutes
# hq <- round(hq/rowSums(hq)*100)

rnd <- runner_liga_03 %>% 
  select(Fraktion, `R&D`) %>% 
  group_by(Fraktion) %>% 
  summarize(Runs = sum(`R&D`, na.rm = T)) %>% 
  spread(Fraktion, Runs)
# rnd <- round(rnd/rowSums(rnd)*100)

archive <- runner_liga_03 %>% 
  select(Fraktion, Archiv) %>% 
  group_by(Fraktion) %>% 
  summarize(Runs = sum(Archiv, na.rm = T)) %>% 
  spread(Fraktion, Runs)
# archive <- round(archive/rowSums(archive)*100)

remote <- runner_liga_03 %>% 
  select(Fraktion, Remote) %>% 
  group_by(Fraktion) %>% 
  summarize(Runs = sum(Remote, na.rm = T)) %>% 
  spread(Fraktion, Runs)
# remote <- round(remote/rowSums(remote)*100)


iron(
  waffle(hq, rows = 5, size = 0.5, title = "Runs auf HQ", pad = 8,
         xlab = paste("1 Waffel = 1 Run; n =", rowSums(hq)),
         colors = c("darkorange", "deepskyblue3", "darkgray", "chartreuse3")),
  waffle(rnd, rows = 5, size = 0.5, title = "Runs auf R&D", pad = 0,
         xlab = paste("1 Waffel = 1 Run; n =", rowSums(rnd)),
         colors = c("darkorange", "deepskyblue3", "darkgray", "chartreuse3")),
  waffle(archive, rows = 5, size = 0.5, title = "Runs auf Archive", pad = 20,
         xlab = paste("1 Waffel = 1 Run; n =", rowSums(archive)),
         colors = c("darkorange", "deepskyblue3", "darkgray", "chartreuse3")),
  waffle(remote, rows = 5, size = 0.5, title = "Runs auf Remotes", pad = 8,
         xlab = paste("1 Waffel = 1 Run; n =", rowSums(remote)),
         colors = c("darkorange", "deepskyblue3", "darkgray", "chartreuse3"))
)
```